
	<h1>{{title}}</h1>

	<ul id="sites">
		{{#each trips}}
		<li>
			<a href="/api/trips/{{this.code}}">
			<img src="{{image}}" alt="Img">
			</a>

			<h2>{{name}}</h2>

			<p>{{description}}</p>

		</li>
		{{/each}}
	</ul>
